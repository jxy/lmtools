{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "${HOME}"],
      "excludeTools": ["write_file", "edit_file", "move_file"]
    }
  }
}
