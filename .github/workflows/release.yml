name: release

on:
  push:
    tags:
      - 'v[0-9]*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      CGO_ENABLED: '0'
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          # Go 1.24 or later stamps the tag into the binary; go.mod names 1.21.
          go-version: '1.26'
          cache-dependency-path: go.mod

      - name: Build
        run: |
          for target in linux/amd64 freebsd/amd64 darwin/arm64; do
            goos="${target%/*}"
            goarch="${target#*/}"
            make build GOOS="$goos" GOARCH="$goarch" BUILD_SUBDIR="$goos-$goarch"
          done

      - name: Check the stamped version
        run: |
          for bin in bin/*-*/lmc bin/*-*/apiproxy; do
            if ! go version -m "$bin" | grep -Eq "^[[:space:]]+mod[[:space:]]+lmtools[[:space:]]+$GITHUB_REF_NAME[[:space:]]*$"; then
              echo "$bin is not stamped with $GITHUB_REF_NAME:"
              go version -m "$bin"
              exit 1
            fi
          done

      - name: Package
        run: |
          mkdir -p dist
          for dir in bin/*-*/; do
            platform=$(basename "$dir")
            tar -czf "dist/lmtools-$GITHUB_REF_NAME-$platform.tar.gz" \
              -C "$dir" lmc apiproxy \
              -C "$GITHUB_WORKSPACE" LICENSE README.md
          done
          (cd dist && sha256sum *.tar.gz > SHA256SUMS)
          ls -l dist

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            gh release upload "$GITHUB_REF_NAME" --clobber dist/*
          else
            set -- --title "$GITHUB_REF_NAME" --generate-notes
            case "$GITHUB_REF_NAME" in
              *-*) set -- "$@" --prerelease ;;
            esac
            gh release create "$GITHUB_REF_NAME" "$@" dist/*
          fi
